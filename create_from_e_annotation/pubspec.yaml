name: create_from_e_annotation
description: Creates an extension method that can generate a subtype from a non-abstract supertype
version: 1.0.0
author: Adrian

environment:  
  sdk: ">=2.6.0 <3.0.0"

dependencies:

dev_dependencies:  
  test: 1.3.4